<UserControl x:Class="Windwaker_coop.Views.ConnectionSetupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <Grid MaxWidth="500" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Game Title -->
        <TextBlock Grid.Row="0"
                   Text="{Binding GameName}"
                   FontSize="28" FontWeight="Bold"
                   Foreground="#FFD700"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,30" />

        <!-- Server / Client Toggle -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,24">
            <RadioButton Content="  Server  " GroupName="Role"
                         IsChecked="{Binding IsServer}"
                         FontSize="16" Margin="0,0,16,0" />
            <RadioButton Content="  Client  " GroupName="Role"
                         FontSize="16" />
        </StackPanel>

        <!-- IP Address -->
        <TextBox Grid.Row="2"
                 Text="{Binding IpAddress, UpdateSourceTrigger=PropertyChanged}"
                 materialDesign:HintAssist.Hint="IP Address"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Margin="0,0,0,8" />

        <!-- Local IP Buttons -->
        <ItemsControl Grid.Row="3" ItemsSource="{Binding LocalIpAddresses}" Margin="0,0,0,16">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding}" Margin="0,0,8,4"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Command="{Binding DataContext.SelectLocalIpCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                            CommandParameter="{Binding}"
                            FontSize="12" Padding="8,4" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Player Name (Client only - hidden when IsServer is true) -->
        <TextBox Grid.Row="4"
                 Text="{Binding PlayerName, UpdateSourceTrigger=PropertyChanged}"
                 materialDesign:HintAssist.Hint="Player Name"
                 Margin="0,0,0,16">
            <TextBox.Style>
                <Style TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsServer}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBox.Style>
        </TextBox>

        <!-- Error Message -->
        <TextBlock Grid.Row="5"
                   Text="{Binding ErrorMessage}"
                   Foreground="Red" FontSize="13"
                   TextWrapping="Wrap"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,16">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ErrorMessage}" Value="">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <!-- Buttons -->
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,0">
            <Button Content="Back" Margin="0,0,16,0"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Command="{Binding BackCommand}" Width="120" />
            <Button Content="Connect"
                    Command="{Binding ConnectCommand}" Width="120">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                        <Setter Property="IsEnabled" Value="True" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsConnecting}" Value="True">
                                <Setter Property="IsEnabled" Value="False" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>

        <!-- Loading indicator -->
        <ProgressBar Grid.Row="7"
                     IsIndeterminate="True"
                     HorizontalAlignment="Center"
                     Margin="0,16,0,0">
            <ProgressBar.Style>
                <Style TargetType="ProgressBar" BasedOn="{StaticResource MaterialDesignCircularProgressBar}">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsConnecting}" Value="True">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ProgressBar.Style>
        </ProgressBar>
    </Grid>
</UserControl>
